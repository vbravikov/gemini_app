/**
 * Default daily nutritional goals.
 * These are used as fallback targets until user-configurable goals are implemented.
 */
export const DEFAULT_DAILY_GOALS = {
  calories_kcal: 2000,
  protein_g: 140,
  carbs_g: 250,
  fats_g: 70,
} as const;
